namespace Roguelike.Utilities {

    //This enumeration is used for the
    //different views that could be shown
    public enum ViewType {
        Menu = 0,
        Settings = 1,
        Game = 2,
        Pause = 3
    }
}
